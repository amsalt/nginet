package internal

// Package internal contains nginet-internal code, to avoid polluting
// the godoc of the top-level nginet package.  It must not import any nginet
// symbols to avoid circular dependencies.
