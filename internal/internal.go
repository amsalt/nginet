package internal

// Package internal contains gnet-internal code, to avoid polluting
// the godoc of the top-level gnet package.  It must not import any gnet
// symbols to avoid circular dependencies.
